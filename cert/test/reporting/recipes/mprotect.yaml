build:
  clean: false
  configuration: recipes/configurations/mprotect.json

systrace:
  enabled: false

deployment:
  local:
    all_attached_devices: true

summary:
  enabled: true
  format: docx
  publish: true
