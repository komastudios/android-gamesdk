build:
  configuration: recipes/configurations/egl_get_frame_timestamps.json

systrace:
  enabled: true
  # custom categories (only supported for local deployment)
  categories: "sched freq idle am wm gfx view input"

deployment:

  ftl:
    enabled: true
