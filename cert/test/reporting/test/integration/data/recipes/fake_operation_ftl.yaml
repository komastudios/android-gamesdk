build:
  clean: false
  release: false
  configuration: test/integration/data/recipes/fake_operation.json

deployment:
  ftl:
    enabled: true
    test: test-robo
    all-physical-devices: true
    flags:
      project: gamesdk-testing
      format: json
    args:
      test-robo:
        type: game-loop
        timeout: 60 # 1 minute

