{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Prediction Spec",
  "description": "A set of predictions to enable.",
  "patternProperties": {
    "[A-Za-z0-9_]*": {
      "description": "The name of the property to enable",
      "type": "boolean"
    }
  },
  "type": "object",
  "examples": [
    {
        "oom_score": true
    }
  ]
}