apply plugin: 'com.android.library'

android {
    compileSdkVersion 29

    defaultConfig {
        versionCode 2
        versionName "0.2"
    }

    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
        }
    }
}
