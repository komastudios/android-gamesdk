apply plugin: 'com.android.library'

android {
    compileSdkVersion 29

    defaultConfig {
        versionCode 1
        versionName "1.0"
    }

    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
        }
    }
}
